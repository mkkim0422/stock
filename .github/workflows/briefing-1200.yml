name: briefing-1200-kst

# KST 12:00 = UTC 03:00. Phase 6 활성.
# on:
#   schedule:
#     - cron: "0 3 * * *"
on:
  workflow_dispatch:

jobs:
  brief:
    runs-on: ubuntu-latest
    if: false
    steps:
      - uses: actions/checkout@v4
      - run: echo "Phase 6 에서 활성"
