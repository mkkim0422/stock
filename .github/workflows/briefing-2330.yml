name: briefing-2330-kst

# KST 23:30 = UTC 14:30. Phase 6 활성.
# on:
#   schedule:
#     - cron: "30 14 * * *"
on:
  workflow_dispatch:

jobs:
  brief:
    runs-on: ubuntu-latest
    if: false
    steps:
      - uses: actions/checkout@v4
      - run: echo "Phase 6 에서 활성"
