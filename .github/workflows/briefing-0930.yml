name: briefing-0930-kst

# KST 09:30 = UTC 00:30. Phase 6 활성.
# on:
#   schedule:
#     - cron: "30 0 * * *"
on:
  workflow_dispatch:

jobs:
  brief:
    runs-on: ubuntu-latest
    if: false
    steps:
      - uses: actions/checkout@v4
      - run: echo "Phase 6 에서 활성"
