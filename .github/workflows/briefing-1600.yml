name: briefing-1600-kst

# KST 16:00 = UTC 07:00. Phase 6 활성.
# on:
#   schedule:
#     - cron: "0 7 * * *"
on:
  workflow_dispatch:

jobs:
  brief:
    runs-on: ubuntu-latest
    if: false
    steps:
      - uses: actions/checkout@v4
      - run: echo "Phase 6 에서 활성"
