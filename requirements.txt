pandas>=2.2
numpy>=1.26
streamlit>=1.35
plotly>=5.20
sqlalchemy>=2.0
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
pytz>=2024.1
tenacity>=8.3
# Phase 2: 실데이터 수집
# pykrx 가 내부에서 pkg_resources(=setuptools)를 import 하므로 명시.
setuptools>=68
pykrx>=1.0.45
yfinance>=0.2.40
finance-datareader>=0.9.90
# Turso libSQL (클라우드 DB) — Streamlit Cloud Python 3.11/3.12 에서만 설치.
# 로컬 Python 3.13+ 에는 호환 wheel 없어 자동 제외 (로컬은 SQLite 사용).
libsql_client>=0.3.1; python_version < "3.13"
